interface:
  display_name: "Code Analyzer"
  short_description: "C/C++ analysis with optional multi-round AI review"
  default_prompt: "Use $code-analyzer with the default Cppcheck, Flawfinder, and Splint analyzers. Enable the independent multi-round ai-review analyzer only if I explicitly request it, then generate and summarize the full offline dashboard."
